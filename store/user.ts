import { userModule } from 'AzgraNuxtLibrary';

export const state = userModule.state;
export const getters = userModule.getters;
export const mutations = userModule.mutations;
export const actions = userModule.actions;
